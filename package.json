{
  "name": "skills",
  "keywords": [
    "CLI",
    "agents",
    "autonomous",
    "skills"
  ],
  "type": "module",
  "scripts": {
    "validate-skills": "bash -c 'for dir in skills/*/; do uvx --from skills-ref agentskills validate $dir; done'",
    "lint": "oxlint --fix",
    "format": "oxfmt --write",
    "check": "bun run --parallel --stream --reporter='append-only' format lint",
    "postinstall": "simple-git-hooks"
  },
  "devDependencies": {
    "@j178/prek": "^0.3.6",
    "oxfmt": "^0.41.0",
    "oxlint": "^1.56.0",
    "simple-git-hooks": "^2.13.1"
  },
  "simple-git-hooks": {
    "pre-commit": "bun prek run --all-files --config='.github/.pre-commit.yml' && bun check"
  }
}
