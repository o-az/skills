[project]
name = "skills"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.14"

[tool.pyrefly]
search_path = [".", "skills/multilingual-caption-video/scripts"]
ignore-missing-imports = ["faster_whisper"]

[tool.ruff]
line-length = 80

[dependency-groups]
dev = ["pyrefly>=1.1.1", "ruff>=0.15.22", "websockets>=16.1"]
